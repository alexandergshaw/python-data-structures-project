# Week 02: Control Flow

## Learning Objectives
- Use conditionals to validate records.
- Identify missing values and duplicates.
- Count records that pass validation rules.

## Assignment Instructions
Implement the starter functions and classes in `assignment.py`. Keep your changes inside this file only so the InsightHub platform can auto-detect completion safely.

## Functions to Implement
- `def find_missing_values(data`
- `def find_duplicates(data`
- `def validate_positive(value`
- `def count_valid_records(data`

## Run Tests
```bash
pytest assignments/week02_control_flow/tests/
```

## Check Completion
```bash
python -c "from assignments.week02_control_flow.assignment import is_complete; print(is_complete())"
```
