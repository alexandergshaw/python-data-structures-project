# Week 07: Arrays and Linked Lists

## Learning Objectives
- Implement a dynamic array wrapper.
- Build linked list nodes and traversal helpers.
- Compare sequential data structures.

## Assignment Instructions
Implement the starter functions and classes in `assignment.py`. Keep your changes inside this file only so the InsightHub platform can auto-detect completion safely.

## Functions to Implement
- `class DynamicArray`
- `def __init__(self) -> None`
- `def append(self, value`
- `def __len__(self) -> int`
- `class Node`
- `class LinkedList`
- `def __init__(self) -> None`
- `def append(self, value`
- `def to_list(self) -> list[Any]`

## Run Tests
```bash
pytest assignments/week07_arrays_linked_lists/tests/
```

## Check Completion
```bash
python -c "from assignments.week07_arrays_linked_lists.assignment import is_complete; print(is_complete())"
```
