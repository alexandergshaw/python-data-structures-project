# Week 09: Recursion

## Learning Objectives
- Sum data recursively.
- Search recursively.
- Flatten nested structures.

## Assignment Instructions
Implement the starter functions and classes in `assignment.py`. Keep your changes inside this file only so the InsightHub platform can auto-detect completion safely.

## Functions to Implement
- `def recursive_sum(data`
- `def recursive_search(data`
- `def flatten_nested(data`
- `def recursive_count(data`

## Run Tests
```bash
pytest assignments/week09_recursion/tests/
```

## Check Completion
```bash
python -c "from assignments.week09_recursion.assignment import is_complete; print(is_complete())"
```
